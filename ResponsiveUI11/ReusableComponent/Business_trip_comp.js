import React from 'react';
import { Text, View } from 'react-native';
import styles from '../Constants/styles';

const Business_trip_comp = ({
    params,
}) => (
    <View style={styles.business_trip}>
        <Text>Business trip cost</Text>
    </View>
);

export default Business_trip_comp;
